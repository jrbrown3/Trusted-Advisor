# Pinned to versions verified working together on Python 3.12.
# (Earlier pins lagged behind what pip actually installs; these match reality.)

# --- Web framework ---
fastapi==0.138.0
uvicorn[standard]==0.49.0

# --- Templating + forms ---
jinja2==3.1.6
python-multipart==0.0.32      # required for form parsing in FastAPI

# --- Database ---
sqlalchemy==2.0.51
aiosqlite==0.22.1             # async SQLite driver
alembic==1.13.2              # schema migrations

# --- Auth ---
PyJWT==2.13.0                      # JWT tokens (replaces python-jose; see security.py)
bcrypt==5.0.0                      # password hashing (direct, not via passlib)

# --- AI providers ---
anthropic==0.111.0
openai==2.43.0

# --- Validation ---
pydantic==2.13.4
pydantic-settings==2.14.2     # settings from .env

# --- Utilities ---
python-dotenv==1.2.2
httpx==0.28.1                # async HTTP
